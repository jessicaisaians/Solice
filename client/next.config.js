/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'https://jobhunt.iran.liara.run', 'http://localhost:4000', 'localhost', 'job-hunt'],
    },
}

module.exports = nextConfig
