'use client'
import {FC} from 'react'

interface LoadingScreenProps{

}

const LoadingScreen: FC<LoadingScreenProps>=({})=>{
        return (<div>LoadingScreen Page</div>);
}
export default LoadingScreen;